# 元の数列
A = [1, 2, 3, 4, 5]

# 累積和を格納するリスト
# S = [0, 0, 0, 0, 0]
S = [0] * len(A)

# 累積和を計算していく
S[0] = A[0]
print(S[0])
# S = [1, 0, 0, 0, 0]
print(S)
# 累積和は最初の値を　計算しないため1からスタート
for i in range(1, len(A)):
    S[i] = S[i-1] + A[i]

print(S)
